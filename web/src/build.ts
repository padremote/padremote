/**
 * When the code this page is running was built.
 *
 * Injected by vite (see `vite.config.ts`). It exists for one question, asked
 * often and answered badly: *is the phone running the current code?* A phone
 * holding a page from before the last change is the most common reason a fix
 * appears not to have worked, and nothing on screen used to say so.
 */
declare const __BUILD__: string;

export const BUILD: string = __BUILD__;
